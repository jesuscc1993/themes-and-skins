## Requirements

1. have `node` installed
2. have `sass` installed globally
3. run `npm install`

## Usage

### Build normally

`npm run build`

### Build in pseudo-compact mode\*

`npm run clean-build`

<small>(\*) removing comments and extra newlines</small>
